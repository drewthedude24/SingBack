# Optional explicit path. If unset, the backend uses system FFmpeg or imageio-ffmpeg.
FFMPEG_BIN=

# Reserved for later checkpoints. The current backend works without either key.
GEMINI_API_KEY=
ELEVENLABS_API_KEY=
