# Optional explicit path. If unset, the backend uses system FFmpeg or imageio-ffmpeg.
FFMPEG_BIN=

# Optional sponsor integrations. The round still completes with deterministic fallbacks.
# Keep real values only in your untracked .env file; never commit them.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=JBFqnCBsd6RMkjVDRZzb
ELEVENLABS_TTS_MODEL=eleven_flash_v2_5
ELEVENLABS_SCRIBE_MODEL=scribe_v2
